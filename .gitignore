/target
# Stable-path install of the blessed capability runner + its content-hash `.blessed` stamp
# (v15 §12.8). Project-local (not $CARGO_HOME/bin) so two checkouts can't fight over one
# blessed binary; regenerated by `just bless`, never committed.
/.vmcell-bin
