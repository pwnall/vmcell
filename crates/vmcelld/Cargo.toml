[package]
name = "vmcelld"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true
license = "MIT OR Apache-2.0"
description = "vmcelld: the blessed vmcell control-plane daemon binary (thin wrapper over vmcell-daemon; design §11)"
publish = false

# Thin binary wrapper (AGENTS.md "functionality in the library, binary is the wrapper"): it blesses
# (refuse-to-start without the three caps, RETAINING them — no uid drop/exec, §11.2), parses the
# server config, and runs `vmcell-daemon`'s server. All logic lives in the library.
[[bin]]
name = "vmcelld"
path = "src/main.rs"

[dependencies]
vmcell-daemon = { path = "../vmcell-daemon", version = "0.9.0" }
vmcell-privilege = { path = "../vmcell-privilege", version = "0.2.0" }
# The setup-broker cutover (§12.4): `fork_privileged_child` splits vmcelld into the capped broker
# child (owns the registry) and the cap-dropped HTTP parent; `libc` for the child's `_exit`.
vmcell-broker = { path = "../vmcell-broker", version = "0.2.0" }
libc = "0.2"
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "signal"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[dev-dependencies]
# The integration tests (tests/integration.rs) spawn a real `vmcelld` through the blessed runner and
# drive it over HTTP — dogfooding the client. `#[ignore]` by default (need KVM + blessed runner +
# artifacts); run via `just test-daemon`.
vmcell-daemon-client = { path = "../vmcell-daemon-client", version = "0.1.0" }
reqwest = { version = "0.13", default-features = false, features = ["rustls"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }
tempfile = "3"
url = "2"
libc = "0.2"
